const settings = require('./settings.json');
const meta = require('./meta.json');
const header = require('./header.json');
const recentWork = require('./recent-work.json');
const caseStudy = require('./case-study.json');

module.exports = {
    settings,
    meta,
    header,
    recentWork,
    caseStudy,
};
